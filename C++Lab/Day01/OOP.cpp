/*
* OOP (C++)
1. Abstraction
    - class (Object blue printer) ⇒ object (class dynamic instance)
    - constructor(생성자), destructor(소멸자), member function, member variable ⇒ 정적 선언방식, 동적 선언 방식
2. Encapsulation
    - 객체 무결성
    - access modifier (public, protected, private)
    - 단, friend 를 이용하여 private member에 직접적 접근을 허용한다
3. Inheritance
    - 확장, 재정의, 객체의 형변환(UpCasting DownCasting)
4. Polymorphism
    - 함수 다형성 : 오버로드, 오버라이드
    - 객체 다형성 : 객체의 형변환 (형변환 지시자)
    - 연산자 다형성 : 연산자 오버로드
- 클래스 정의시 타입을 결정하는 것이 아니라 객체의 사용시 타입을 결정하는 기법
template => STL
*/